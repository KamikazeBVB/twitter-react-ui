import {expect} from 'chai';
import {getTweets} from './../TwitterClient';

describe('Helper: Tweeter Client Tests', () => {
  it('Should run the test', () => {
    expect(true).to.equal(true);
  });
});